# Copy this file to config.yaml and adjust only the non-secret settings you need.
# Before startup, export OPENROUTER_API_KEY in the shell environment.

provider:
  # Supported values: openrouter
  # Default: openrouter
  name: openrouter
  # Required for openrouter.
  model: openai/gpt-4.1-mini
  # Default: 60.0
  timeout_seconds: 60.0

logger:
  # Relative paths are resolved from the project root.
  # Default: logs
  log_dir: logs
  # Supported values: debug, info, warning, error
  # Default: info
  log_level: info

mcp:
  # Optional. Controls which MCP tools are registered.
  # Supported values: get_persona, get_user_profile, search_memory, ingest_turn
  # Default: all tools enabled
  enabled_tools:
    - get_persona
    - get_user_profile
    - search_memory
    - ingest_turn

memory_search:
  # Optional. Controls how many memory hits search_memory returns.
  # Default: 3
  top_k: 3
  # Future direction only, not implemented today:
  # memory_indexing:
  #   chunk_size: 500
  #   chunk_overlap: 50

storage:
  # Required. Relative paths are resolved from the project root.
  data_dir: data

persona:
  # Required. Relative paths are resolved from the project root.
  dir: personas
  # Required. Loads <dir>/<name>.md
  name: default
